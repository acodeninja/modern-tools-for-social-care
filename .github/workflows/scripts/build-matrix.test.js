const buildMatrix = require('./build-matrix');

describe('building a matrix based on folder structure', () => {
  test('returns a list of folders', async () => {
    expect(await buildMatrix()).toBe([

    ])
  })
})
